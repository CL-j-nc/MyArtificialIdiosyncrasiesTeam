{
  "name": "myartificialidiosyncrasiesteam",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Static site — no build needed'",
    "dev": "npx wrangler pages dev ."
  }
}
